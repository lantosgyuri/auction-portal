package command_service

func StartListener() {
	// Create set of commands for eventTypes
	// For example bid check if finished, store, publish update auction event
	// Subscribe to channels
	// Create Validator
	// Create EventCreator
	// Let request validated by Validator
	// If error response to queue
	// Save event in DB
	// If error response to queue
}
